// DO NOT EDIT THIS FILE MANUALLY IN NORMAL CAPACITOR WORKFLOWS.
// It is scaffolded here so the Android Studio project already knows the native Capacitor plugins.

android {
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation project(':capacitor-android')
    implementation project(':capacitor-app')
    implementation project(':capacitor-haptics')
    implementation project(':capacitor-splash-screen')
    implementation project(':capacitor-status-bar')
    implementation project(':capacitor-community-admob')
}

if (hasProperty('postBuildExtras')) {
    postBuildExtras()
}
